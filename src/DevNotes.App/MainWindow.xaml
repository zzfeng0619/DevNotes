<Window x:Class="DevNotes.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:DevNotes.App.ViewModels"
        mc:Ignorable="d"
        Title="DevNotes - 本地开发笔记" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <viewModels:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <ToolBarTray Grid.ColumnSpan="2" Grid.Row="0">
            <ToolBar>
                <Button Content="新建笔记"
                        Margin="4,0"
                        Padding="8,2"
                        Command="{Binding NewNoteCommand}" />
                <Button Content="保存"
                        Margin="4,0"
                        Padding="8,2"
                        Command="{Binding SaveNoteCommand}" />
                <Button Content="删除"
                        Margin="4,0"
                        Padding="8,2"
                        Command="{Binding DeleteNoteCommand}" />
            </ToolBar>
        </ToolBarTray>

        <!-- 左侧笔记列表 -->
        <Border Grid.Row="1"
                Grid.Column="0"
                Margin="8"
                BorderBrush="#CCC"
                BorderThickness="1"
                CornerRadius="4">
            <DockPanel>
                <TextBlock Text="笔记列表"
                           FontWeight="Bold"
                           Padding="8,6"
                           Background="#F3F3F3"
                           DockPanel.Dock="Top" />
                <ListBox ItemsSource="{Binding Notes}"
                         SelectedItem="{Binding SelectedNote, Mode=TwoWay}"
                         DisplayMemberPath="Title"
                         Margin="4"
                         HorizontalContentAlignment="Stretch">
                </ListBox>
            </DockPanel>
        </Border>

        <!-- 右侧编辑区域 -->
        <Border Grid.Row="1"
                Grid.Column="1"
                Margin="8"
                BorderBrush="#CCC"
                BorderThickness="1"
                CornerRadius="4">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,8">
                    <TextBlock Text="标题" Margin="0,0,0,2" />
                    <TextBox Text="{Binding SelectedNote.Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,6"
                             FontSize="14" />

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Text="创建时间：" />
                        <TextBlock Text="{Binding SelectedNote.CreatedAt}" Margin="4,0,16,0" />
                        <TextBlock Text="最后更新：" />
                        <TextBlock Text="{Binding SelectedNote.UpdatedAt}" Margin="4,0,0,0" />
                    </StackPanel>
                </StackPanel>

                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="内容（Markdown 文本）" Grid.Row="0" Margin="0,0,0,4" />

                    <TextBox Grid.Row="1"
                             Text="{Binding SelectedNote.Content, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             TextWrapping="Wrap"
                             FontFamily="Consolas"
                             FontSize="13" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
